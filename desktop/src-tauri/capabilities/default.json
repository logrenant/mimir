{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the WebView may ask the Rust shell for. Deliberately short: the daemon endpoint command is our own, and the folder picker is the only plugin surface exposed.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
